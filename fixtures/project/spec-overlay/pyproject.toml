[project]
name = "overlay-demo"
version = "0.3.0"
description = "Project carrying its own .indexion/kgfs overlay spec"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
