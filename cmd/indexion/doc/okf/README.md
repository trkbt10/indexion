# okf

## API

- **`first_prose_line`** (Function) — First prose line of a text, truncated for display. Lines that read as
- **`default_bundle_title`** (Function) — Derive a bundle title from the analysis root: its directory name, or the
- **`strip_external_prefix`** (Function) — Strip the first matching KGF resolver prefix (e.g. a `bare_prefix`)
- **`prose_prefix`** (Function) — Lines of a text before its first markdown heading, trimmed.
- **`default`** (Function) — Create a default OKF export configuration.
- **`OkfConfig`** (Struct) — Configuration for OKF bundle export.
- **`command`** (Function) — Define the okf command for argparse.
- **`resolve_internal_target`** (Function) —
