import {
  "moonbitlang/core/encoding/utf8" @utf8,
  "moonbitlang/x/fs" @fs,
  "trkbt10/indexion/src/config" @config,
}

supported_targets = "native"

options(
  "native-stub": [ "parallel_stub.c" ],
)
