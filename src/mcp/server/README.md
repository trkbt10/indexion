# server

## API

- **`ToolHandler`** (Struct) — A registered tool handler. Takes JSON params, returns a result.
- **`McpServer`** (Struct) — The MCP server. Holds registered tools and dispatches requests.
- **`RegisteredTool`** (Struct) — A registered tool: definition + handler.
