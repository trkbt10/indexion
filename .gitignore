.DS_Store
_build/
.mooncakes/
.moonagent/
.docgen/
.indexion/*
!.indexion/wiki/
!.indexion/.gitignore
# .claude: global gitignore excludes the .claude directory entirely,
# so files under skills/ require `git add -f` to track.
# settings.local.json and worktrees/ must never be committed.
.claude/settings.local.json
.claude/worktrees/
node_modules/
dist/
out/
*.vsix
*.tgz
/coverage/
packages/*/coverage/
.vscode-test/
packages/graph-canvas/scripts/screenshots

# transpile-todo-app integration test writes target outputs here for
# inspection and per-language compiler verification.
tests/integration/transpile-todo-app/output/
